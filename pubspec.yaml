name: dr_map
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_riverpod: ^3.0.0
  flutter_svg: ^2.2.1
  riverpod_annotation: ^3.0.0

dev_dependencies:
  build_runner:
  custom_lint:
  riverpod_generator: ^3.0.0
  riverpod_lint: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/svgs/
    - assets/svgs/map_assets/
    - assets/svgs/provinces/
